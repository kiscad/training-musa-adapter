# 2026-09-22 源码审查与优化

本轮基线为 `f53f2fa`，开始时工作区干净。按用户要求不运行测试用例、smoke、
训练或性能基准；新增回归仅供后续执行。历史验收范围仍见 [PATCH_LEDGER](PATCH_LEDGER.md)，
不以静态检查替代数值、硬件或完整训练验收。

## 审查范围

通读 `src/training_musa_adaptor` 的 35 个 Python 文件，覆盖配置与入口、版本门控、
import watcher、补丁注册/应用/撤销、设备适配、attention/GDN 分发，以及 TE、Megatron、
mcore-bridge、transformers 各补丁模块；结合相关测试、README、贡献指南、配置示例、
CI 脚本和台账核对契约。未为整齐而移动模块、重排注册表或批量格式化未修改文件。

## 已修复的问题

| 问题与触发条件 | 修复与保留的行为 | 待执行回归 |
|---|---|---|
| 已安装目标在晚注册重建时，所有工厂转为拒绝；引擎删除清理记录但旧 replacement 留在目标上 | 删除记录前恢复原绑定，继承属性删除本包覆盖；恢复写入加入原有回滚日志，别名跟随恢复 | `test_engine_lifecycle.py`：普通属性、继承 staticmethod、别名、撤销 |
| 配置冻结后修改 DEBUG，日志函数重新解析环境，可能在目标提交后抛错 | DEBUG 从冻结配置读取，避免状态与实际绑定分离；启动总开关机制不变 | `test_engine_lifecycle.py`：冻结后非法 DEBUG 不影响应用与撤销 |
| 晚注册的版本表达式非法时，补丁尚未登记但 suite 映射已写入 | 将 suite 映射更新放在全批次门控校验之后；测试清理同时清空映射，避免跨用例污染 | `test_engine_lifecycle.py`、`tests/conftest.py` |
| 架构能力补丁在 CPU/CUDA 环境也返回模拟架构 | 无可用 MUSA 时工厂/Hook 拒绝；MUSA 上保持固定 8.3/8 与所有权撤销 | `test_device_arch.py` |
| FSDP PREMUL_SUM 替代分支未限制设备，并丢失上游 `no_grad` 约束 | 仅 MUSA 梯度进入预缩放，在 `no_grad` 内原地缩放；CPU/CUDA、AVG、bf16 等仍交给原函数 | `test_distributed.py`：设备透传、叶子张量、梯度模式恢复 |
| 非 MUSA attention 在透传前仍访问适配配置、CP group 等；列表形式 `[-1, 0]` 绕过因果窗口一致性检查 | 非 MUSA 立即原参数透传；列表/元组窗口使用同一判定 | `test_attention.py` |
| GDN 的形状谓词只检查单个归一化形状，不能发现 q/k/v/g/beta 不一致、非法 GVA heads 或状态形状 | 分发前以元数据检查完整输入关系，不一致时原参数交给 FLA；保留合法 GVA | `test_ssm.py` |
| GDN packed offsets 仅差分后转 int，未检查首尾、batch、整数 dtype 与单调性 | 验证 `[0, total_tokens]` 覆盖和 batch=1；在已有 host 读取上计算差分，移除设备 `torch.diff` 调用，不增加同步次数 | `test_ssm.py`：偏移首尾、浮点/逆序偏移、合法 packed |
| ATen RoPE 工厂把任意非 TE 函数当成可替换对象；apex 加载吞掉 ABI/内部依赖错误 | 保留已有的 None/已知 Megatron TE wrapper 接入条件，保护其他第三方实现；仅顶层可选包/扩展缺失时拒绝，损坏错误传播 | `test_rope.py` |
| MoE topk 代理默认向原函数传 `dim=None`，与原生类型声明的末维默认值不一致 | 默认参数改为 `-1`，含 `out=` 透传；显式 dim 参数、FP64 计算与梯度路径不变 | `test_moe.py` |
| 补丁维护说明仍引用已删除开关，unit-tests 脚本注释误称只跑轻量用例 | 改为按补丁 ID 禁用，修正架构值可配置的错误说明；脚本注释说明真实全量收集行为 | 文档/源码静态核对 |

既有 teardown 测试把成功返回值断言为 None，而当前 `_install_clean_teardown` 明确返回
True；修正断言为 True，保留重复安装拒绝及 undo 行为断言。Hook 协议仍只有 False 表示拒绝。
未删除测试、放宽数值容差或新增 skip/xfail。

## 源码依据与兼容范围

本轮不新增补丁 ID，不改变 target、requires、注册顺序、version_gates 或配置 schema。
修复集中于本包边界与既有契约；没有重新认证全部历史版本门控。

- 相邻 `../torch_kernels` checkout（需单独取得）revision
  `afc9fe027866a6fab5c7d3d8b18ae82dcd25e084`：
  `torch_kernels/attention/gated_delta_net.py` 的 `_check_inputs`、`_adapt`、packed 文档，
  以及 `attention/tilelang/flash_linear_attention/gdn_shapes.py`。
  前者约束输入间关系，后者只约束归一化形状；本轮在调用前补齐前者所需条件。
- 安装的 torch 2.7.1：
  `/usr/local/lib/python3.10/dist-packages/torch/_C/_VariableFunctions.pyi` 的 topk 声明
  为 `dim: int = -1`，文档也明确省略 dim 时使用末维。
- 相邻 `../Megatron-LM` checkout（需单独取得）：
  `megatron/core/distributed/fsdp/src/megatron_fsdp/param_and_grad_buffer.py` 的
  `gradient_reduce_preprocessing`。以下发行源码均保留 `@torch.no_grad()` 与
  `gradient_reduce_div_fusion && dtype != bf16` 的 PREMUL_SUM 分支：

| tag | commit |
|---|---|
| core_v0.14.0 | `23e00ed0963c35382dfe8a5a94fb3cda4d21e133` |
| core_v0.15.0 | `0d7e02bd26d3eb347ad8d1bef625002babfa6df7` |
| core_v0.16.0 | `3bec9aa97dda898d16ff5a89bac0ed2b6682b172` |
| core_v0.16.1 | `55ac7082517c3878ae653c07c09c534b8aed49f6` |
| core_v0.17.0 | `9539a12e1b04a68423f57b3eb41d6125161dca24` |
| core_v0.17.1 | `266f1c97dca477ca8b92c16087506da2000b0b84` |
| core_v0.18.0 | `ba7b5ebce12af60627a80985792a1449ce45f46c` |
| core_v0.18.2 | `571370c829ca768fe37244f4e2e7f28d8accc4ab` |
| core_v0.19.0 | `5be9626709af2722333bf54797c954c09edeada3` |

这份对照仅证明本次修复保留的函数约束；没有修改既有 `>=0.14,<0.20` 门控，
也不把未列出的 patch release 或未知后续版本记为本轮已核查。

## 验证与后续验收

审查解释器 Python 3.10.12，静态工具 Ruff 0.16.8 / Black 26.5.1 / isort 5.13.2 /
mypy 2.3.1。仅读取发行元数据：torch/torch-musa 2.7.1、TE 2.0.0+651a47b7、
torch-kernels 0.1.0；未导入这些包检查运行来源或初始化设备，因此这些信息不是硬件验收环境。

执行的检查：

- `TRAINING_MUSA_ADAPTOR_ENABLED=0 TORCH_DEVICE_BACKEND_AUTOLOAD=0 bash scripts/ci/lint.sh`：
  **通过，退出码 0**；Ruff、Black、isort 无问题，mypy 检查 35 个源码文件无错误。
- Python 3.10 AST 语法解析 `src/` 和 `tests/`：**70 个文件通过，退出码 0**，不执行模块或用例。
- `git diff --check`、`bash -n scripts/ci/unit-tests.sh`：**通过，退出码 0**。
- 新增/修改文档的 **69 个本地 Markdown 链接通过检查**；AST 对照确认修改文件的
  patch ID、target、trigger、requires、version_gates、别名范围未变化，均退出码 0。

测试执行状态：**全部未运行，按用户要求留待用户执行**。
后续全量测试应包括本轮回归，并在匹配 MUSA 栈上复验 attention/GDN/RoPE 前后向、
FSDP 多 rank 梯度归约及恢复。引擎相关用例重点覆盖晚注册、reload、继承、别名和失败回滚。
台账中原有 TE 原生 DPA、modeling-first、完整训练入口与恢复、CUDA graph 等缺口未因此关闭。
未测量吞吐、显存或端到端延迟，移除一次 GDN 设备差分不构成训练提速承诺。
