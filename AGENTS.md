# Coding agent 开发与维护规范

适用于本仓库及其子目录。开始任务前阅读本文；修改某个目录前检查其中更具体的约定。
遵守平台/上级指令与用户本次要求。已有明确授权范围内的可逆工作直接推进，不为常规
读代码、编辑或测试反复请求确认；实质改变用户目标或缺少必要外部信息时才澄清。

本文规定开发流程和验收要求，不宣称所有现有实现已满足全部目标。机制详见
[CONTRIBUTING](CONTRIBUTING.md)，使用方式见 [README](README.md)，实际验收状态只在
[补丁台账](docs/PATCH_LEDGER.md)维护。架构背景从 [docs/design.md](docs/design.md)
进入，历史记录不能替代当前源码与本轮测试。

## 1. 项目目标与完成定义

在明确验证的框架、软件栈、硬件和负载内，让原始训练源码、入口和配置在 MUSA 上正确
运行，并选择适用实现。交付同时考虑正确性、可追踪性、性能代价和后续维护成本。

- “进程未崩溃”不等于数值正确；前向正确不等于梯度、优化器、RNG 或恢复正确。
- 单元桩测试不等于设备验收；版本门控命中不等于该版本全部支持。
- 单算子加速不等于训练提速；局部 smoke 不等于完整框架兼容。
- 目标能力未实现、条件不具备、未运行、skip 和真实失败必须区分。
- 本次任务达到明确目标且相应检查通过才结束。无法验证的部分保留为明确限制，
  不通过降低功能、修改上游断言或改写台账把它变成“完成”。

## 2. 工作开始：先保护现场，再建立事实

按以下顺序执行，并把检查范围控制在当前任务需要的文件内：

1. 阅读用户最新要求、当前文档与适用约定；将任务写成可验证结果，例如
   “指定 MUSA 输入 force=mate 不支持时抛错”而非“优化兼容性”。
2. 运行 `git status --short`、查看必要的 diff 和最近历史，区分已有用户修改与本轮修改。
   脏工作区不是停止理由；保留现有暂存、未暂存和未跟踪文件，不 reset、clean、stash
   或覆盖来获得干净基线。需要隔离验证时使用临时目录或独立 checkout。
3. 用 `rg` 定位真实调用链、补丁记录、配置和对应测试；先读后改。不要据旧 review、
   文件名或设计目标推断当前行为，也不要重复执行先前已完成的工作。
4. 涉及兼容性时记录发行包版本、实际 import 文件路径、源码 revision/build、硬件和
   触发条件。先确认正在测试哪个副本，尤其是 wheel、editable 和 `PYTHONPATH` 混用时。
5. 对 bug 建立最小复现或能捕获错误的回归；对性能问题先建立可重复基准；对文档任务
   先核对当前 API、脚本和台账，不启动不相关训练。
6. 简要说明发现、拟改范围与验证方式。长任务持续报告实际进展、失败原因和下一步，
   不只更新计划，也不把猜测当结论。

未要求提交时保留可审查的工作区 diff，不自行 commit/push。用户已授权的后续动作无需
再次询问。不要修改本任务以外的训练产物、上游仓库或安装源码来使测试通过。

## 3. 架构约束

### 3.1 保持普通补丁简单

- 普通适配只用 `AttrPatch` / `HookPatch`、普通函数和显式 `PATCHES`。
  一个文件能读到目标、工厂、条件和维护原因；新增它通常不需要改引擎。
- 不引入 PatchSpec/BindingSpec/ProviderSpec、Action/MutationPlan、通用 Planner、
  动态插件发现或统一算子协议。新增抽象须有两个真实调用方、现有普通函数无法清楚表达
  的具体问题，以及减少维护成本的证据；不以假想扩展为理由。
- 单调用点逻辑留在补丁内。提取到 `ops/` 或 `backends/` 的函数要有真实共享语义与测试；
  不为消除几行相似代码把不同框架契约强行统一。
- `_engine.py` / `_imports.py` 不导入 torch、框架或 kernel，不增加模型、设备或框架名称
  特判；已知外部引擎冲突检测属于生命周期兼容边界，不扩展为业务路由。
- 不复制整份上游模块，独立维护本包的适配实现，避免引入会激活其他补丁引擎的依赖或别名。
  发行名 `training-musa-adaptor`、包名 `training_musa_adaptor`、变量前缀
  `TRAINING_MUSA_ADAPTOR_` 保持一致。

### 3.2 导入、绑定与生命周期

- 补丁模块顶层只依赖标准库和本包轻量定义；torch、框架、kernel 在工厂或经过审查的
  安全边界加载。bootstrap 不解析完整 TOML、不加载 packaging 或加速库。
- AttrPatch 工厂只返回 replacement，不能自己写目标。`None` 是主动拒绝，异常不能
  被当作 None；Hook 的拒绝值则是 False，失败时须自行清理部分副作用。
- 捕获传入的原始句柄，不从被替换的公开函数重新查找入口。保留签名、返回结构、
  descriptor/继承、tensor subclass、dtype/device 和非 MUSA 行为。
- Hook 仅在真实 exec 边界执行；finder 只查找/包装，namespace 根包不是 Hook 边界。
  不新增全局 eager import 扫描来弥补漏掉的导入时机。
- `requires` 仅引用同模块不同属性的 AttrPatch；不允许循环、跨模块或同目标依赖。
  消费者不自动启用前置。不 import 兄弟补丁或读取其私有状态制造隐式依赖。
- 同目标顺序有语义，不能随意排序；别名修复默认关闭，有证据才指定最小前缀。
- 重复 install/apply 不叠 wrapper；提交前和撤销时检查所有权；第三方后写对象保留。
  回滚/undo 失败保留清理记录并阻止直接重装，不增加 `except: pass` 掩盖状态破坏。
- 事务是每目标范围，不承诺整个 import 的全局回滚。无 undo 的 Hook、外部库注册、
  编译缓存等不可逆效果如实记录。训练中并发切换、设备初始化后的 fork 不作为默认能力。

### 3.3 配置、版本和错误

- 配置仅来自默认值、显式 TOML、环境变量，按此顺序覆盖；同源专项优先，列表整体替换。
  总开关硬退出，未知值报错，首次相关执行边界冻结；显式 install 立即冻结。
- ONLY 非空时忽略 DISABLE；两个列表仍校验，前置不隐式启用。不得另造配置优先级、
  per-patch 环境旋钮或运行时任意对象修改入口。
- ONLY/DISABLE 条目可以是注册的套件名（`SUITES`，如 `megatron`），展开为该套件
  全部补丁 ID；套件映射随补丁注册传入，未注册套件名按未知 ID 报错。
- 新建补丁、修改目标/调用契约或调整 `version_gates` 前，必须对照所适配开源仓库
  不同版本的实际源码，核对目标及调用方，按契约推导范围；不能只看本机版本、发行说明
  或照抄其他补丁的 gate。接口变化时采用必要的独立变体，不能为消除失败随意扩大范围、
  删除上界或添加生产用全局忽略开关。
- 当前 Megatron 门控策略见
  [`patches/megatron/__init__.py`](src/training_musa_adaptor/patches/megatron/__init__.py)。
  源码核对范围与真实设备验证范围分别记录；元数据缺失、源码探针未知均不是成功证明。
- 可选包确实不存在可按声明回退；其内部依赖损坏、ABI/JIT 初始化错误和执行异常保留
  原始原因。不得给整个工厂或训练步骤套宽泛 try/except 后悄悄换实现。
- 自动入口保护普通 import torch 的异常边界不能扩散到后续框架导入和目标应用。

## 4. 训练正确性与性能原则

| 领域 | 必须守住的契约 |
|---|---|
| 数值 | 参数/输入 dtype、autocast、输出提升、设备、shape/stride、别名与原地语义、前向/反向 |
| 训练 | dropout/RNG、checkpoint 重算、梯度累积、参数更新、确定性与辅助返回值 |
| Attention | dense/packed、offset/padding/空序列、mask/causal/window、GQA、QK/V head dim、CP/TP |
| GDN | 单独核对线性注意力语义、状态、变长布局与梯度，不套普通 attention 假设 |
| 通信 | 各参与 rank 协议一致、collective 顺序、超时和异常退出，不能按本地条件独立降级 |
| checkpoint | 参数、optimizer、RNG、并行分片与新进程恢复后的继续训练结果 |
| compile / graph | eager 通过不代表编译或捕获通过；加载/JIT/预热在捕获前，图内不改策略 |

- 不以关闭用户要求的功能、修改 mask/dropout/精度/并行计划换取运行成功。既有 workaround
  的触发条件、性能代价和移除依据继续保留，不因此成为无条件支持承诺。
- 候选支持检查在执行前完成，优先使用元数据，不试跑 kernel/collective 或消耗 RNG。
  新增 `.cpu()`、`.item()`、同步、全量扫描或每步读配置需说明不可避免的原因与测量结果。
- force 不降级，upstream 保留原参数，非 MUSA 输入保留原路径；kernel 抛错后不换实现重跑。
  no_grad 不能单独用于放宽 backward 能力，因为重算可能需要梯度。
- 调度缓存不能持有训练 tensor，不以 tensor 值或不断变化的 shape 建立无界缓存。
  诊断有界，默认不逐调用刷日志，不在 report 中触发设备初始化或通信。
- 性能比较固定输入、栈、同步/预热与重复方法，记录前后向、转换/JIT、峰值显存和回退比例。
  没有本轮基准就不写“提升吞吐”；历史数字保留日期与原适用条件。

## 5. 实施流程与测试选择

### 5.1 修改前确定最小验收

为每个改动明确：问题、影响目标、必须保留的契约、预期前后行为、最小验证及必要的硬件
范围。先给相关行为补回归，再完成最小实现；纯文档、重命名等低影响变更不为凑覆盖率
新增模拟实现细节的测试。已有测试能表达契约时优先扩展它。

改动按问题收敛，避免顺手重排注册表、批量格式化无关文件或把搬家、算法替换、配置变更
混在一起。需要机制调整时先解释为何局部补丁解决不了，再补公共机制回归。

### 5.2 开工与验证

在已有开发环境内使用以下入口；不要把这些命令当成每个文档任务都要执行的固定清单：

```bash
git status --short
git log --oneline -3
python3 -m pip install --no-deps -e .
python3 -m training_musa_adaptor list
bash scripts/ci/quick-check.sh
bash scripts/ci/pre-push.sh
```

运行依赖与质量工具需事先可用，`--no-deps` 不安装它们。工具版本/范围以
[pyproject.toml](pyproject.toml)、[CI workflow](.github/workflows/quality.yml) 和
[scripts/ci](scripts/ci) 为准。可选执行 `bash scripts/setup-dev-hooks.sh` 安装本地 hooks；
这是工作区配置操作，不是验证结果，也不能代替 CI。

`pre-push.sh` 跑 lint + pytest；其中 `unit-tests.sh` **实际收集整个 tests**，无 marker 过滤。
有设备时部分测试会用硬件，部分子进程还会主动开启自动加载。先读所选测试的环境条件，
不要仅凭文件名或 musa/integration marker 推断成本与覆盖。

| 变更 | 必要验证范围 |
|---|---|
| 文档 | 本地链接、命令/API/ID/路径、示例语法及配置解析、当前实现与台账一致 |
| 局部补丁 | 工厂成功/拒绝、非 MUSA、原参数透传、异常、独立性；计算行为变化须真实数值/梯度 |
| 引擎/导入 | engine、lifecycle、activation、导入顺序、find_spec/namespace/LazyLoader、descriptor/继承、requires、别名/reload、失败回滚、第三方改写与不可逆项 |
| 配置/版本/选择 | precedence、冻结、错误输入、ONLY/DISABLE、门控边界、force/upstream、支持拒绝不执行、执行失败不重试 |
| 通信/恢复 | 多 rank 正常/异常/超时，以及新进程 checkpoint 恢复等价性；单元桩不足以关闭验收项 |
| 发布元数据/入口 | wheel 内容与新进程安装/自动激活/总开关；检查实际 import 来源与遗留 entry point |

硬件轮次的入口示例：

```bash
TMA_RUN_INTEGRATION=1 MEGATRON_LM_PATH=/path/to/Megatron-LM \
  python3 -m pytest tests -q
```

只在资源与依赖具备时运行；记录实际设备、版本、退出码和各类结果。每个测试带适当 marker
及明确条件，复用 conftest 清理状态。全 skip 不等于通过；CI 绿色不补齐未执行的硬件范围。
先做当前任务所需测试，公共机制改动扩大回归；通过后不无理由反复运行重型测试。

### 5.3 失败处理

1. 保留原始异常和命令，确认代码副本、配置、版本与资源，再判断是本轮回归、既有问题
   还是外部条件；不先修改断言。
2. 若是自己的回归，修复根因，重跑失败用例及有证据受影响的范围。
3. 既有问题记录最小复现与影响，必要的同范围修复可继续完成；无关问题不扩张成另一项任务。
4. 依赖或硬件不足时完成不依赖它们的检查，明确剩余验收；不要把“测试已编写”写成通过。
5. 不删除上游测试、不新增 skip/xfail 掩盖失败、不放宽容差消除数值差异。确需修正本项目
   错误断言时说明旧断言为何违背契约，以独立证据支撑新断言。

## 6. 常见任务的操作要求

### 新补丁或新框架

定位独立调用点 → 证明缺口 → 选择 AttrPatch（仅必要时 Hook）→ 写完整维护字段与门控
→ 明确注册 → 补相关回归 → 更新逐 ID 台账 → 验证实际入口。
新增框架的自动激活至少覆盖 torch-first 与 framework-first；确认没有 Megatron 时仍可用。
mcore-bridge 与 Megatron-Bridge 是不同项目，不混用名称、模块路径或验证结果。

### 上游/厂商版本升级

核对发行与实际源码 → 比较目标及调用者契约 → 调整能力判断或划分独立变体 → 测试旧/新
边界和预发布/厂商后缀 → 运行适用硬件范围 → 分别登记源码核对与硬件验收。
不将某个接口未变推广成整套框架已支持，不为适配库安装而自动升级 vendor 栈。

### version_gates 的必做核查

新补丁和版本范围变更都执行以下流程，详细方法见 CONTRIBUTING 第 4 节：

1. 定位上游发行 tag/分支对应的 commit SHA，确认发行包名与源码映射；厂商 fork 另记
   build 和相关差异。优先使用本地参考 checkout，缺失版本再查上游固定提交源码。
2. 阅读拟定下界及相邻更早版本、范围内各发布线、最新拟支持版本，以及已知变化边界
   两侧的目标与调用方源码。结合 diff/历史检查中间版本、patch release/backport，核对
   签名、默认值、返回值、内部语义、配置字段、Hook 时机和原问题是否仍存在；不只比符号名。
3. 按契约首次成立确定下界，按契约破坏/目标移除/补丁不再适用确定排除边界；未知后续
   版本采用有说明的保守核查边界，注明它不是已证实的不兼容点。不从当前运行版本反推
   全部范围，不把未获取源码的版本记为已核实。中间例外明确排除或拆分变体；依赖包分别核对。
4. 补充门控上下界、区间内外、相关预发布/厂商后缀及变体重叠/空洞测试，并运行对应
   目标契约回归。仅版本比较通过不证明补丁适用，仅单版本硬件通过也不证明跨版本兼容。
5. 在补丁附近写明范围理由，在逐 ID 台账或其链接记录中保存“版本/tag/SHA → 目标与
   调用方源码 → 契约变化 → 纳入/排除理由 → 实际测试”的对照。缺证据时保持未核实，
   不宣布版本适配完成。源码核查范围、硬件验证范围和完整训练范围分别交付。

### 重构与优化

保留公共 ID、配置语义、激活顺序与历史计算修复，除非本次明确改变且有变更说明。
先确定可观测行为和相关基线，再移动或收敛实现；删除被替代的死路径，不长期保留两套活跃
引擎或配置协议。优化以测量决定，新增复杂度必须有可解释的收益。

### 补丁退役

在新进程禁用补丁复现原问题并通过 → 检查消费者、前置、别名和配置引用 → 移除实现、注册
及专用死代码 → 保留有用行为回归 → 更新台账与旧 ID 的配置影响说明。
不复用旧 ID 表示完全不同的行为；上游修复声明不代替本栈验证。

## 7. 文档与证据维护

| 变更内容 | 同步位置 |
|---|---|
| 用户可见配置/安装/行为 | README.md；英文入口 README.en.md 的对应说明 |
| 机制、作者接口与开发步骤 | CONTRIBUTING.md；必要时设计入口/架构基线 |
| Agent 流程、质量门禁和维护约束 | AGENTS.md，与实际 scripts/ci 一致 |
| patch 新增、变体、退役与验证 | docs/PATCH_LEDGER.md 的明确 ID 条目 |
| 一次性 review/实验 | 带日期与环境的记录；链接台账，不复制另一张“最新支持表” |

文档默认中文。英文入口不可宣称更宽支持范围；原始软件标识保持准确拼写。
用户与开发文档按独立项目介绍当前目标、接口与流程，不要求读者了解其他适配项目或演进历史。
源码归属集中保留在 NOTICE；外部引擎冲突检测及其测试中的真实标识不得为清理文案而删除或伪造。
不要将历史设计中的将来时搬成已实现事实，不复制已失效旧命令。仓库外参考链接标明需要
相邻 checkout；核心上手和贡献流程应在单独检出本仓库时仍能阅读。

测试报告记录软件版本/build、源码来源、设备、shape/dtype、并行配置、命令与退出码。
长日志、模型、checkpoint、profile 和临时实验放到任务临时目录，不混入源码提交；只保留
足以复现结论的简洁记录。引入新运行时依赖必须解释必要性、许可证/维护影响和导入时机，
不把系统环境依赖偷偷加入自动升级链。

## 8. 交付前自审与完成门槛

检查完整 diff，特别是配置优先级、导入时机、非 MUSA 路径、第三方所有权和异常路径。
确认未扩大无证据的 gate/能力，未留下重复注册、未使用工具函数、调试 print 或旁路开关。

针对当前任务完成以下核对，不以无关检查增加成本：

- 实现解决原问题；对应反例与成功路径均有证据，测试不只复述实现。
- 需要的质量门禁及相关测试已执行；未执行项与原因写明，没有假称测试通过。
- `git diff --check` 无本轮新增问题；原有用户暂存、未暂存内容未丢失。
- 新增或变更的 `version_gates` 有上游多版本源码对照、上下界理由和边界测试，
  证据能定位到 commit；未把本机通过或未知未来版本写成整个区间支持。
- 示例使用实际存在的 API、配置和路径；台账没有把待验收项升级为已支持。
- 普通补丁仍易于独立阅读、禁用和退役；机制新增有明确必要性。

最终交付说明主要结果、原因、验证和限制，附关键文件。不要让用户阅读所有中间日志才知道
是否完成。需要继续验收的事项写成具体输入/环境/行为，不用“以后优化”代替明确缺口。

```text
问题与结果：
关键修改及保留的契约：
version_gates 源码核查范围、上下界理由与证据位置：
验证环境、命令、退出码、通过/失败/跳过：
性能证据（或未测）：
台账/文档：
尚未完成的验收与原因：
```
